import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

/** Wrapper letting long content shrink inside flex/grid parents. @const TooltipWrapper */
export const TooltipWrapper = styled(Box)({
  minWidth: 0,
});
